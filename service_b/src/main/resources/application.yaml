spring:
  application:
    name: service_b

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

service-c:
  serviceCUrl: ${SERVICE_C_URL}
  saveUrl: /api/save

app:
  message:
    important-type: important